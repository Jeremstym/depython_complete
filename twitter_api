import os
from requests_oauthlib import OAuth1Session
import pandas as pd
import tweepy

f = open("C:/Users/Luca/Desktop/Boulot/ENSAE/depythons/keys.txt")
keys = f.read().split("\n")
f.close()

consumer_key = keys[0]  #API key
consumer_secret = keys[1]  #API secret key

auth = tweepy.OAuthHandler(consumer_key, consumer_secret)
auth.set_access_token(access_token, access_token_secret)
api = tweepy.API(auth)
